scenamatica: v1.0.0

name: sc001
description: L1C1 in scenarios.xlsx
on:
  - type: manual_dispatch

scenario:
  - type: execute
    action: message_send
    with:
      content: "Hello, World!"
      recipient: SaikyouPeyangsan

